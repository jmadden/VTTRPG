{
  "name": "vtt",
  "version": "0.1.0",
  "private": true,
  "description": "Local self-hosted, system-agnostic 2D Virtual Tabletop",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "db:init": "psql \"$DATABASE_URL\" -f backend/db/schema.sql",
    "db:seed": "psql \"$DATABASE_URL\" -f backend/db/seed.sql",
    "db:setup": "npm run db:init && npm run db:seed",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w backend && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "concurrently": "^9.1.0",
    "playwright": "^1.61.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
